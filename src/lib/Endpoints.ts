export enum ParasutEndpoints {
    BASE_URL = "https://api.parasut.com",
    VERSION = "/v4",
    TOKEN_URL = "/oauth/token",
    GET_SALES_INVOICES = "/sales_invoices",
}

export enum HTTPMethods {
    GET = "get",
    POST = "post",
}
